# slv_visual — https://www.slv-visual.online
#
# Stance: SPLIT. Retrieval and citation are welcome; training on this content
# is not. Search engines and AI answer engines may crawl the site and quote it
# when answering someone; crawlers that exist to build training corpora may not.
#
# NOTE FOR WHOEVER EDITS THIS NEXT: Cloudflare prepends a managed block to this
# file at the edge (AI Crawl Control / Content Signals). The live robots.txt is
# therefore longer than this one — always check the deployed URL, not the repo.
# That managed block already sets Content-Signal: ai-train=no and disallows the
# training crawlers. The disallow rules below repeat it deliberately, so that
# turning the Cloudflare setting off cannot silently flip the site to
# fully-open without anyone noticing.

User-agent: *
Allow: /
Disallow: /admin
Disallow: /admin.html
Disallow: /api/
Disallow: /uploads/

# --- AI answer engines: retrieval and citation, explicitly allowed -----------
# These fetch a page to answer a question and cite the source. That is traffic
# and attribution, so they are welcome.

User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: Claude-Web
Allow: /

User-agent: PerplexityBot
Allow: /

# --- Training crawlers: not allowed -----------------------------------------
# These collect content into training corpora. Nothing comes back to the site,
# so they are refused. Cloudflare's managed block says the same thing; this is
# here so the policy survives a dashboard change.

User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: Amazonbot
Disallow: /

User-agent: meta-externalagent
Disallow: /

Sitemap: https://www.slv-visual.online/sitemap.xml
